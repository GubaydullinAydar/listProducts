spring:
  data:
    mongodb:
      database: list_products_db
  mvc:
    path match:
      matching-strategy: ant_path_matcher
springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger
